# Observations — agentic-research

Dated log per the conventions.

## 2026-07-18 — four launch runs (parallel)

- **Cost-spread hypothesis part-falsified**: prediction was ±20% across skills on the same model; actual spread was baseline 83.9k → spartan 113.3k → daymade 141.9k tokens (+69% over baseline). Orchestration has a real token price — though daymade's includes a persistent evidence trail (52-source citation registry, per-subagent notes) no other variant produces.
- **Model contrast, first look**: sonnet (spartan) wrote ~15,400 words vs fable (spartan) ~6,600 on the same prompt — a 2.3× verbosity gap at similar token cost. Fable spent tokens on verification-per-word; sonnet on words. Verbosity bias controls will matter in this comparison.
- **Harness constraint discovered**: a session-wide WebSearch cap (200/200) exists and was hit by the daymade run mid-pipeline (it degraded gracefully to direct WebFetch). Running four research agents in one parallel window shares that budget — future multi-run windows should stagger or budget searches per run.
- **Recurring Write-denial quirk** hit every run again (heredoc fallback held). Worth a permissions fix before runs that can't fall back.
- **Prompt encoding worked**: all four outputs produced TL;DR-first summaries, early definitions, per-approach subdirectories, and owner-tailored recommended-steps — the qualities that vanished in second-brain-original when unrequested. Prompt-level encoding of the preferences doc held across skills and models.
- **Convergent meta-finding across all four runs**: every variant independently identified the same core ingredients (plan-first, parallel subagents with context isolation, effort scaling, citation verification, rubric-based judging) and flagged that no independent head-to-head evaluation of research systems exists — the gap this repo's harness sits in.

## 2026-07-18 — four-way comparison (v1 fallback judge)

- **Judge ran as the v1 fallback** (Claude judging Claude) under the owner's provider hold — first comparison filed under the hold; queued for v2 re-judging when the Google API account arrives.
- **First order-swap disagreement on an overall verdict**: pass 1 (read A→D) picked daymade; pass 2 (read D→A) picked spartan-fable, demoting daymade for disclosed coverage gaps ("disclosed absence is still absence"). Two-report comparisons so far had perfect agreement — four-way ranking appears materially more order-sensitive, and the passes even weighted honesty-about-gaps in opposite directions (pass 1 credits it under instruction-following, pass 2 refuses to let it offset coverage). The protocol's flag-don't-average rule is earning its keep.
- **Dimension-level agreement was still solid**: both passes agree daymade leads synthesis + citation quality, baseline `/research` is most readable but thinnest on evidence, spartan-sonnet is the hardest to read. The disagreement is confined to how the stable dimension picture should aggregate.
- **Blinding was harder than previous comparisons**: the topic *is* research skills, so reports legitimately name the skills under test as survey subjects; three explicit self-identifications had to be neutralized (daymade's pipeline banner ×2, spartan-fable's header, plus one self-referential aside in spartan-sonnet). Content mentions of skill names were left — a judge can't be blinded to the field itself.
- **Spot-check near-ceiling**: 19/20 supported, 1 partial (an understatement, not an inflation), 0 unsupported. At this citation-fidelity level the spot-check stops discriminating between variants — the differentiator was attribution precision, which the rubric's citation dimension already captured. Future experiments may need a harder verification probe (e.g., sampling only conflicting-number claims) to regain signal.
- **Verbosity did not buy verdicts**: the two longest reports (spartan-sonnet 15.3k words, daymade 18k) did not sweep either pass; the 6.6k-word spartan-fable won pass 2 outright. Encouraging sign against verbosity bias, worth tracking across comparisons.

## 2026-07-18 — human verdict + loop iteration

- **Human verdict: spartan-fable preferred** (matches pass 2 — the first pass–human agreement in the harness's history, after 0-for-4). The owner's stated reason is formatting mechanics, not apparatus: daymade "writes with no paragraph breaks" and its inline citation lists are "unreadable… each should be on its own line". Depth was explicitly valued; presentation was decisive.
- **The rubric's readability dimension under-specified what the owner means by readable**: both judge passes noted daymade's [n]-registry reference-hopping, but neither named paragraph structure or citation-list layout — the things the owner saw first. Loop proposal filed to codify these in research-preferences.md and the judge briefing.
- **Owner follow-ups**: full non-degraded daymade run (unblocked operationally by the new one-variant-per-session rule; filing blocked on the re-run-versioning decision, which is deliberately open on the map) and better-formatted daymade output (vendored skill can't be edited — the paths are prompt-level formatting rules and/or a `daymade-deep-research+<writing-skill>` stack variant).

## 2026-07-18 — daymade --r2 (own-session re-run, headless dispatch)

- **The quota starvation was an artifact, confirmed**: with its own session, the pipeline completed all six research tasks (the original lost d–f), producing a 55-source registry (vs 52) with every source live-fetched — including the community/developer-tooling coverage the original disclosed as missing.
- **New deviation discovered in the dispatch route**: a headless `claude -p` session doesn't get the native WebSearch/WebFetch tools granted. The skill's degraded-mode provisions handled it — curl-based DuckDuckGo search and curl fetches inside subagents, fully disclosed in its execution log — but r1→r2 retrieval differences now confound with the search-engine change. Headless dispatch needs a web-tools allowance (or an interactive fresh session) before it's the standard route for research runs.
- **First automated cost/token capture for a Claude Code run** (an open map question, now part-answered): the headless JSON reports duration 11m27s, 116,151 conversation tokens (+3.03M cache reads), **$25.11 API-equivalent**. Orchestration's dollar price is now a datum, not a guess. Caveat: subscription billing may differ from API-equivalent pricing.
- **Formatting unchanged, as predicted**: same canonical prompt (held for comparability), so the [n]-registry style and dense paragraphs the owner's verdict flagged persist in r2 — visible in its summary's method-disclosure paragraph. The formatting fix rides on preferences-encoded prompts (future experiments) or a writing-stage stack, not on re-running.
- **Faster solo than shared**: 11m27s vs the original's 20m37s in the four-run window — parallel windows don't just share search quota, they appear to contend on throughput too.

## 2026-07-18 — first writing-stage stack run (daymade+pocock-edit-article, from r2)

- **The stack mechanism works**: reusing r2's output as the intermediate isolated the writing delta perfectly — the edit stage's input is byte-identical to a filed run, so any quality difference between r2 and this run is attributable to `pocock-edit-article` alone. This "derived stack" pattern (`--from-r2`) cost ~110k tokens vs ~$25-equivalent for a fresh research stage.
- **The 240-char rule directly answers the owner's verdict**: longest prose paragraph across all six edited files is 239 chars (was: multi-hundred-char walls, including a ~1,500-char TL;DR now split into eight paragraphs plus a recipe list). Reference lists stayed one-per-line; dense benchmark sentences became bulleted claim-then-evidence pairs.
- **Fidelity held at write time**: citation-marker sets and URL counts identical input→output on all six files (independently re-verified: zero new URLs); internal links resolve. The comparison-time stage-fidelity spot-check still applies when this run is judged.
- **Edit-stage scoping decision worth standardizing**: article-oriented edit skills get the article-like files only; resources lists and evidence trails pass through byte-identical. If more stack runs happen, this scope rule belongs in the conventions rather than per-run deviations.
- **Two harness quirks**: the Write tool's report-file guard blocked a direct `summary.md` write (staged via scratchpad and copied — same family as the recurring Write-denial quirk from the launch runs); and the skill's HITL "confirm sections" gate needed auto-approval, like daymade's gates — vendored HITL skills consistently need an AFK-gate policy in dispatch instructions.

## 2026-07-18 — writing-stage comparison (r2 vs edit stack, v1 fallback judge)

- **Cleanest comparison the harness has run**: with the research stage byte-identical, both judge passes independently reported "the same underlying content in two formattings" and tied every substance dimension — the design isolated formatting exactly as intended, and a two-report pairwise comparison restored full inter-pass agreement (pairwise now 3-for-3; only the four-way split).
- **Both passes picked the edited report**, on readability (6→8 and 6→9) plus a slight instruction-following edge in pass 2 (a scannable TL;DR is itself an instructed behavior). The judges' rationale tracks the owner's verdict language almost point for point — evidence the readability judge-briefing (loop PR #37) landed.
- **New writing-stage failure mode found by the fidelity check — "citation re-stamping"**: 5/8 claims faithful, 3/8 drifted, 0 invented. All three drifts are one mechanism: splitting a long sentence re-stamps its citation onto the fragments, sometimes extending attribution to editorial text that was uncited in the intermediate, or narrowing a modifier's scope. Meaning and numbers survived everywhere; attribution silently spread. **Standardize in future edit-stage dispatches: never add a citation marker to text that lacked one.**
- **Residual headroom both judges flagged**: the [n]-registry style survived editing (the dispatch permitted converting to in-flow links; the stage barely used it) and the edit occasionally over-fragments into choppy one-sentence paragraphs. A future edit-stage variant (or our own skill, in a later map) has a clear target.
- Live citation check 5/5 supported — consistent with the four-way's near-ceiling finding that live checks no longer discriminate; the stage-fidelity check carried the verification signal this time.
